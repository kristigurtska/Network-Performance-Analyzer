# Network Performance Analyzer

A simple web application that measures network request performance using JavaScript's Performance API.

---

## 📌 Overview

The **Network Performance Analyzer** is a small browser-based tool that measures how long a network request takes to complete.
It sends a request to a public API and calculates the response time in milliseconds using JavaScript.

This project demonstrates basic networking concepts and asynchronous programming in a simple and interactive way.

---

## ✨ Features

* Analyze network request performance
* Measure response time using `performance.now()`
* Uses JavaScript Fetch API
* Error handling for failed requests
* Clean and minimal user interface

---

## 🛠 Technologies Used

* HTML5
* CSS3
* JavaScript (ES6)
* Fetch API
* Browser Performance API

---

## 🚀 How to Run

1. Clone the repository:

   ```bash
   git clone https://github.com/YOUR-USERNAME/network-performance-analyzer.git
   ```

2. Open the project folder.

3. Open `index.html` in your browser.

No installation or additional setup required.

---

